jQuery(document).ready(function($) {
    $('#lightSlider').lightSlider({

        item:1,
        adaptiveHeight:true,
        slideMove:1,
        speed:100,
        slideMargin:30,
        loop:true,
        rtl:true,

    });
});